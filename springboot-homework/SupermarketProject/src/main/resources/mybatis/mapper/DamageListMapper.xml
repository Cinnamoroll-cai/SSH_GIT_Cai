<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.seiryo.admin.mapper.DamageListMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.edu.seiryo.admin.pojo.DamageList">
        <id column="id" property="id" />
        <result column="damage_date" property="damageDate" />
        <result column="damage_number" property="damageNumber" />
        <result column="remarks" property="remarks" />
        <result column="user_id" property="userId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, damage_date, damage_number, remarks, user_id
    </sql>
   <select id="getNextDamageNumber" resultType="java.lang.String">
	    SELECT MAX(damage_number) AS damageNumber
	    FROM t_damage_list
	    WHERE TRUNC(damage_date) = TRUNC(SYSDATE)
	</select>
	
	<!-- 
	<select id="damageList" resultType="com.edu.seiryo.admin.pojo.DamageList">
	SELECT * FROM (
	    SELECT TMP.*, ROWNUM ROW_ID
	    FROM (
	        select dl.*,u.user_name as userName
	        from t_damage_list dl
	        left join t_user u on dl.user_id = u.id
	        <where>
	            <if test="null != damageListQuery.startDate and damageListQuery.startDate !='' ">
	                and TRUNC(damage_date) &gt;= TO_DATE(#{damageListQuery.startDate},'yyyy-MM-dd')
	            </if>
	            <if test="null !=damageListQuery.endDate and damageListQuery.endDate !='' ">
	                and TRUNC(damage_date) &lt;= TO_DATE(#{damageListQuery.endDate},'yyyy-MM-dd')
	            </if>
	        </where>
	        ORDER BY dl.damage_date DESC
	    ) TMP
	    WHERE ROWNUM &lt;= #{page.offset} + #{page.size}
	)
	WHERE ROW_ID &gt; #{page.offset}
	</select>
	
	
	 -->
    

</mapper>
