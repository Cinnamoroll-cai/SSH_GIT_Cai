<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.seiryo.admin.mapper.OverflowListMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.edu.seiryo.admin.pojo.OverflowList">
        <id column="id" property="id" />
        <result column="overflow_date" property="overflowDate" />
        <result column="overflow_number" property="overflowNumber" />
        <result column="remarks" property="remarks" />
        <result column="user_id" property="userId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, overflow_date, overflow_number, remarks, user_id
    </sql>
    <select id="getOverflowNumber" resultType="java.lang.String">
	    SELECT MAX(overflow_number) AS overflowNumber
	    FROM t_overflow_list
	    WHERE TRUNC(overflow_date) = TRUNC(SYSDATE)
	</select>
    
    <!-- 
    <select id="overFlowList" resultType="com.edu.seiryo.admin.pojo.OverflowList">
	SELECT * FROM (
	    SELECT TMP.*, ROWNUM ROW_ID
	    FROM (
	        select ol.*,u.user_name as userName
	        from t_overflow_list ol
	        left join t_user u on ol.user_id = u.id
	        <where>
	            <if test="null != overFlowListQuery.startDate and overFlowListQuery.startDate !='' ">
	                and TRUNC(overflow_date) &gt;= TO_DATE(#{overFlowListQuery.startDate},'yyyy-MM-dd')
	            </if>
	            <if test="null !=overFlowListQuery.endDate and overFlowListQuery.endDate !='' ">
	                and TRUNC(overflow_date) &lt;= TO_DATE(#{overFlowListQuery.endDate},'yyyy-MM-dd')
	            </if>
	        </where>
	        ORDER BY ol.overflow_date DESC
	    ) TMP
	    WHERE ROWNUM &lt;= #{page.offset} + #{page.size}
	)
	WHERE ROW_ID &gt; #{page.offset}
	</select>
    
     -->
	
</mapper>
